pipeline{
    agent any
    environment {
        PATH = "$PATH:/opt/maven/bin:$PATH"
    }
    stages{
       stage('Clone Code'){
            steps{
                git branch: "master" , 'https://github.com/venkateshnk28/mavenjava.git'
            }
         }        
       stage('Build '){
       de
            steps{
                sh 'mvn clean install'
            }
         }
        stage('SonarQube analysis') {
//    def scannerHome = tool 'SonarScanner 4.0';
        steps{
        withSonarQubeEnv('sonarqube10.3') { 
        // If you have configured more than one global server connection, you can specify its name
//      sh "${scannerHome}/bin/sonar-scanner"
        sh "mvn sonar:sonar"
    }
        }
        }
       
    }
}
